[package]
name = "dprint-plugin-toml"
version = "0.7.0"
authors = ["David Sherret <dsherret@gmail.com>"]
edition = "2021"
homepage = "https://github.com/dprint/dprint-plugin-toml"
keywords = ["formatting", "formatter", "toml"]
license = "MIT"
repository = "https://github.com/dprint/dprint-plugin-toml"
description = "TOML formatter for dprint."

[lib]
crate-type = ["lib", "cdylib"]

[profile.release]
opt-level = 3
debug = false
lto = true
debug-assertions = false
overflow-checks = false
panic = "abort"

[features]
wasm = ["serde_json", "dprint-core/wasm"]
tracing = ["dprint-core/tracing"]

[[test]]
name = "specs"
path = "tests/spec_test.rs"
harness = false

[dependencies]
dprint-core = { version = "0.69.0", features = ["formatting"], default-features = false }
dprint-core-macros = "0.1.0"
serde = { version = "1.0.145", features = ["derive"] }
serde_json = { version = "1.0", optional = true }
thiserror = "2"

[dev-dependencies]
dprint-development = "0.11.0"
serde_json = { version = "1.0" }
